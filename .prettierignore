# Build output and dependencies — never formatted by hand.
node_modules/
.next/
out/
dist/
build/
.cache/

# Generated by npm, not by hand: `npm install` rewrites it, so formatting it
# here would be undone on the next install.
package-lock.json

# Generated artifact: rewritten by scripts/prove-phase3.ts from a live testnet
# run. Reformatting it would make the recorded proof diverge from what the
# script actually wrote.
tests/fixtures/phase3-proof.json

# husky's generated hook shims.
.husky/_
